// src/constants/planner.ts

/**
 * Soft cap per day: maximum number of activities per day.
 * Used ONLY during initial distribution on distributeRoundRobin
 */
export const MAX_ACTIVITIES_PER_DAY = 5;
